PORT=4001
FRONTEND_ORIGIN=http://localhost:3001
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=dev
DB_PASS=devpass
DB_NAME=vocahub
TYPEORM_LOGGING=false
JWT_ACCESS_SECRET=replace-with-a-long-random-access-secret
JWT_REFRESH_SECRET=replace-with-a-long-random-refresh-secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
# Required only when upgrading a database with decks that have no owner.
# Create the target user first, then set this to that user's id.
LEGACY_USER_ID=
